using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class debugGenericIndicator : MonoBehaviour
{
    public GameObject gravityArea;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Vector3 planetVelocity = gravityArea.GetComponent<Rigidbody>().velocity;
        //Debug.Log(planetVelocity.magnitude);
    }
}
